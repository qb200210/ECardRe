<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="../../favicon.ico">

    <title>KnoWell -- Know Your Connections Well</title>

    <!-- Bootstrap core CSS -->
    <link href="../dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="../assets/css/jumbotron.css" rel="stylesheet">
    
    <script type="text/javascript" src="https://platform.linkedin.com/in.js">
		    api_key: 75x21kb6ah0nxy
		</script>
		
		<script type="text/javascript">
				function submitAsSignIn(){				
					$('#email').attr('required');	
					$('#password').attr('required');	
					document.getElementById('signForm').action = '/login';
					$('#phatomBtn').trigger('click');
				}
				
				function submitAsSignUp(){					
					$('#email').attr('required');	
					$('#password').removeAttr('required');
					document.getElementById('signForm').action = '/signup';
					$('#phatomBtn').trigger('click');
				}
						    
		    // Setup an event listener to make an API call once auth is complete
		    function onLinkedInLoad() {
	    		// remove required input file
    			$('#email').removeAttr('required');
					$('#password').removeAttr('required');
	        IN.UI.Authorize().place();
	        IN.Event.on(IN, "auth", getProfileData);
		    }
		
		    // Handle the successful return from the API call
		    function onSuccess(data) {
		       console.log(data);
		    }
		
		    // Handle an error response from the API call
		    function onError(error) {
		        console.log(error);
		    }
		
		    function loadMember(profile){
					member = profile.values[0];
					document.getElementById('signForm').action = '/linkedinSignin';
					console.log(profile);	
					posArray = member.positions.values;
					companyName = "";
					title = "";
					for (var i=0; i<=posArray.length-1; i++) {
						if (posArray[i].isCurrent){
							companyName = posArray[i].company.name;
							title = posArray[i].title;
						}
					}
					document.signForm.linkedinObj.value = member.firstName + ",," + member.lastName + ",," + member.emailAddress + ",," + member.pictureUrl + ",," + member.location.name + ",," + companyName + ",," + title + ",," + member.id;
					console.log(document.signForm.linkedinObj.value);	
					document.getElementById('signForm').submit();
		    }
		
		    // Use the API call wrapper to request the member's basic profile data
		    function getProfileData() {
		       // IN.API.Raw("/people/~").result(onSuccess).error(onError);
		       IN.API.Profile("me").fields(["id","firstName", "lastName", "emailAddress", "pictureUrl", "location", "positions"]).result(loadMember);
		    }
		
		</script>
  </head>

  <body>

    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="javascript:void(0)">KnoWell</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <form class="navbar-form navbar-right" id="signForm" name="signForm" action='/login' method='POST'>
            <div class="form-group">
            	<input id="email" name="email" type="email" class="form-control" placeholder="Email" required>
            </div>
            <div class="form-group">
            	<input id="password" name="password" id="password" type="password" class="form-control" placeholder="Password" required>
            </div>
            <input type="hidden" name="_csrf" value="<%= csrf_field %>" style="display: none;"/>
            <input type="hidden" name="linkedinObj" value=""  style="display: none;"/>
            <button id="phatomBtn" type="submit" class="btn btn-success" style="display: none;">Phatom</button>
            <button type="button" class="btn btn-success" onclick="javascript:submitAsSignIn()">Sign in</button>
            <button type="button" class="btn btn-warning" onclick="javascript:submitAsSignUp()">Sign Up</button>
            <small style="padding: 0 10px 0; color: #ffffff">Or, with</small><a href="javascript:onLinkedInLoad();"><img id="linkedinBtn" src="../assets/img/linkedin_mini.png" style="width: 30px; height: 30px"></a>
          </form>
        </div><!--/.navbar-collapse -->
      </div>
    </nav>

    <div class="jumbotron" style="margin: 0 0 0; background: #ff5500">
      <div class="container">
      	<div class="row">
	      	<div class="col-md-4">
				    <img class="media-object img-responsive" src="../assets/img/demo1.JPG" alt="...">
				  </div>
	        <div class="col-md-8">
				    <h1>Hello, world!</h1>
	        	<p>This is a template for a simple marketing or informational website. It includes a large callout called a jumbotron and three supporting pieces of content. Use it as a starting point to create something more unique.</p>  
	        </div>
				</div>
      </div>
    </div>
    
    <div class="jumbotron" style="margin: 0 0 0; background: #91addd">
      <div class="container">
        <div class="row">
	      	<div class="col-md-4 col-md-push-8 col-sm-4 col-sm-push-8">
				    <img class="media-object img-responsive" src="../assets/img/demo2.JPG" alt="...">
				  </div>
	        <div class="col-md-8 col-md-pull-4 col-sm-8 col-sm-pull-4">
				    <h1>Hello, world!</h1>
	        	<p>This is a template for a simple marketing or informational website. It includes a large callout called a jumbotron and three supporting pieces of content. Use it as a starting point to create something more unique.</p>  
	        </div>
				</div>
      </div>
    </div>
    
    <div class="jumbotron" style="margin: 0 0 0; background: #64b5f6">
      <div class="container">
      	<div class="row">
	      	<div class="col-md-4">
				    <img class="media-object img-responsive" src="../assets/img/demo1.JPG" alt="...">
				  </div>
	        <div class="col-md-8">
				    <h1>Hello, world!</h1>
	        	<p>This is a template for a simple marketing or informational website. It includes a large callout called a jumbotron and three supporting pieces of content. Use it as a starting point to create something more unique.</p>  
	        </div>
				</div>
      </div>
    </div>



    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="../assets/js/jquery.min.js"></script>
    <script src="../dist/js/bootstrap.min.js"></script>
  </body>
</html>
